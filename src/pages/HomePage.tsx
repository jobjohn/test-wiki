import WikiList from '../components/WikiList';

const HomePage = () => {
  return (
    <div className="home-page">
      <main>
        <WikiList />
      </main>
    </div>
  );
};

export default HomePage; 